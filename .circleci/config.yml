jobs:
  test:
    docker:
      - image: python:2.7.16 
    steps:
      - checkout
      - run: python create_db.py 
      - run: python tests.py

workflows:
  version: 2
  default:
    jobs:
      - test
